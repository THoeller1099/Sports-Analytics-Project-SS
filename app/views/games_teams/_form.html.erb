<%= form_with(model: games_team, local: true) do |form| %>
  <% if games_team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(games_team.errors.count, "error") %> prohibited this games_team from being saved:</h2>

      <ul>
        <% games_team.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :team_id %>
    <%= form.number_field :team_id %>
  </div>

  <div class="field">
    <%= form.label :game_id %>
    <%= form.number_field :game_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
